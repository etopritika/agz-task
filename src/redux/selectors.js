export const selectUsers = (state) => state.users.users;
export const selectPage = (state) => state.users.page;
export const selectTotalPages = (state) => state.users.total_pages;
export const selectToken = (state) => state.users.token;
export const selectPositions = (state) => state.users.positions;
export const selectIsLoading = (state) => state.users.isLoading;
